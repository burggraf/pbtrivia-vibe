# Network URL for QR codes and external access
# Replace with your machine's IP address on your local network
# Find your IP with: ipconfig getifaddr en0 (Mac) or ifconfig (Linux) or ipconfig (Windows)
# This ensures QR codes work when scanned from mobile devices
VITE_NETWORK_URL=http://192.168.1.117:5173
